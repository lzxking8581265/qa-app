# 服务器配置
server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: api-recorder
  
  datasource:
    url: jdbc:mysql://localhost:3306/api_recorder?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useServerPrepStmts=false&cachePrepStmts=false&rewriteBatchedStatements=false
    username: root
    password: first@YD
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false  # 20250904 - 禁用Hibernate SQL日志输出
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: false  # 20250904 - 禁用SQL格式化输出
        
  security:
    user:
      name: admin
      password: admin

# 日志配置
logging:
  level:
    com.example.api: INFO  # 20250904 - 改为INFO级别
    org.springframework.security: INFO  # 20250904 - 改为INFO级别
    org.hibernate.SQL: WARN  # 20250904 - 改为WARN级别，禁用SQL日志
    org.hibernate.type.descriptor.sql.BasicBinder: WARN  # 20250904 - 改为WARN级别
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# OpenAPI配置
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

# Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: health,info
      base-path: /actuator
  endpoint:
    health:
      show-details: always

# 性能监控配置
app:
  performance:
    monitoring:
      enabled: false  # 是否启用性能监控 - 已禁用
      slow-query-threshold: 100  # 慢查询阈值(毫秒)
      log-slow-queries: false     # 是否记录慢查询日志 - 已禁用
      log-performance-metrics: false  # 是否在响应中包含性能指标 - 已禁用
